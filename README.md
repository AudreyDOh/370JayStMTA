# Jay St-Metrotech MTA Data

Real-time train arrivals at Jay St-Metrotech (A, C, F, R, N, Q).

Open `jayst_metrotech_data.html` in a browser. Use a local server if needed (e.g. `python3 -m http.server 8000`).
